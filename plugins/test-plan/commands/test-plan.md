---
description: 実装前にタスクをテストケース一覧(テスト計画)に落とし、仕様担保の基準にする。可能なら失敗するテストの雛形も作る
allowed-tools: Bash, Read, Grep, Glob, Write, Edit
---

これから実装するタスクのテスト計画を作成してください。引数: $ARGUMENTS(タスクの説明。省略時は会話の文脈・PR本文・参照されたチケットから把握する)

テスト計画は「このタスクが正しく終わったと言える条件」を **テストとして実行できる形** で並べたものです。/test-check が実装後にこの計画と実際のテストを突合します。

## 手順

1. タスクの内容を把握する。優先順位: $ARGUMENTS → 会話の文脈 → `gh pr view --json title,body`(ドラフト PR があれば) → 参照されたチケット・Slack・Notion の依頼文 → 関連する既存仕様(docs/, README, CLAUDE.md)
2. リポジトリのテスト環境を確認する: テストフレームワーク(pytest / jest / vitest / playwright 等)、テストの置き場所、実行コマンド(package.json / Makefile / CLAUDE.md から)。**分からなければ推測せず「要確認」に書く**
3. 不明点が実装の成否に関わる場合だけ、計画を書く前にユーザーに質問する(最大3問。細部は「要確認」として計画に残してよい)
4. 現在のブランチ名を取得し、`.claude/specs/<ブランチ名のスラッシュを-に置換>.md` に以下の形式で書く。**このファイルはリポジトリにコミットしない**(正式な記録は Notion 側)。書く前に `.git/info/exclude` に `.claude/specs/` が無ければ追記する:
   ```bash
   EXCL="$(git rev-parse --git-dir)/info/exclude"; grep -qxF ".claude/specs/" "$EXCL" 2>/dev/null || echo ".claude/specs/" >> "$EXCL"
   ```

```markdown
# テスト計画: <タスク名>
作成: <日付> / ブランチ: <ブランチ名>
テスト実行: `<コマンド>` / 置き場所: <パス>

## 目的(1〜2行)

## テストケース
各ケースは「前提 / 操作 / 期待結果」で、外から観測できる振る舞いを書く。
- [ ] TC-1 [結合] <正常系: 〜のとき〜になる>
- [ ] TC-2 [結合] ...
- [ ] TC-3 [単体] <純粋ロジック: 〜の計算が〜になる>
- [ ] TC-4 [結合] <エッジ: 空・0件・境界値・重複・同時実行・途中失敗>
- [ ] TC-5 [結合] <既存動作の維持: 〜は変わらない>
- [ ] TC-6 [E2E] <happy path: 画面で〜して〜が表示される>

## 変更してはいけないこと(非目標)
- NG-1: ...

## 要確認(仕様が曖昧な点)
- Q-1: ...
```

5. 書いた計画をユーザーに提示し、**承認を得る**
6. 承認後、テスト環境が判明していれば、TC ごとに **失敗する(または skip 状態の)テストの雛形** を作ることを提案する。テスト名またはコメントに `TC-n` を含める(/test-check が機械的に対応づけられるように)。作るかどうかはユーザーの判断に従う
7. `${CLAUDE_PLUGIN_ROOT}/scripts/events-log.sh test_plan.created cases=<TC数>` でログに記録する(スクリプトが無ければ `find ~/.claude/plugins -path '*test-plan*' -name events-log.sh | head -1`)

## テストの配分(トロフィー型)

書くテストの種類は次の優先順で決める。「どの層で検証するか」を各 TC に明記する。

| 層 | 何を検証するか | 量 |
|---|---|---|
| **結合(主力)** | API ルート / ビュー / Server Action / コマンドを、実 DB(テスト用)や実モジュールと繋いで呼び、レスポンスと保存結果を見る。外部 SaaS だけモック | TC の大半 |
| 単体 | 純粋なロジック(計算・変換・判定)だけ。I/O を伴うものは結合で見る | 必要な分だけ |
| E2E | ユーザーが画面で行う **happy path を 1〜2 本**。異常系は結合で | 1〜2 |
| 静的 | 型チェック・lint は既存の CI に任せる(計画に書かない) | — |

- E2E は Playwright を前提にし、Playwright MCP が使える環境では実行の様子を GIF に残せる旨を計画に書く(`.claude/e2e/<branch>/`)
- 既存のテスト基盤(fixture、factory、テスト DB の作り方)を先に Grep で探し、計画の「テスト実行」と「置き場所」に反映する

## ルール

- テストケースは「コードがどう書かれるか」ではなく「外から観測できる振る舞い」で書く
- 1タスクのケースは 5〜12 個程度。それを超えるならタスク分割を提案する
- 実装中に仕様が変わったら、コードではなく **先にこのファイルを更新** する
- 既存のテストで既にカバーされている振る舞いは、そのテスト名を添えて「既存」と書く(新しく書かない)
